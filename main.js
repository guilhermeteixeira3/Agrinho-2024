// Função para navegar até a página de projetos
function navigateToProjects() {
    window.location.href = 'page2.html';
}

// Adicionar event listeners para qualquer elemento interativo aqui
document.addEventListener('DOMContentLoaded', () => {
    // Código para ser executado quando a página carregar
});
